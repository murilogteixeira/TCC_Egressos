import 'package:flutter/material.dart';

class ListaDetalhesWidget extends StatelessWidget {
  const ListaDetalhesWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          Container(
            color: Colors.black,
            height: 20,
          )
        ],
      ),
    );
  }
}
